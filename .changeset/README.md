# Changesets

Run `pnpm changeset` for changes that should release `@tevm/cli` or
`tevm-run`. Commit the generated Markdown file with the implementation.

The release workflow turns pending changesets into a release pull request and
publishes packages after that pull request is merged.

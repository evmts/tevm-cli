---
title: Terminal playground
description: Watch the tevm CLI and tevm-run work — an animated, self-contained tour of the most useful commands.
---

import { CommandExplorer } from '../components/CommandExplorer'

# Terminal playground

Every `tevm` subcommand runs a real in-process EVM. Pick one below to see what it does
and what its output looks like — everything renders locally in this page, nothing
leaves your browser.

<CommandExplorer />

:::info
These demos are simulated recordings. Install the CLI with
`npm install --global @tevm/cli` to run them for real.
:::
